class AppConstants {
  AppConstants._();

  static const appName = "Quanttora";

  static const animationDuration = Duration(
    milliseconds: 300,
  );

  static const cardElevation = 0.0;

  static const appBarHeight = 60.0;

  static const defaultPadding = 20.0;

  static const defaultRadius = 18.0;

  static const maxContentWidth = 650.0;

  static const bottomNavHeight = 72.0;
}